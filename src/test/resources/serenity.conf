serenity {
  project.name = "Bootcamp Screenplay Framework"
  take.screenshots = "DISABLED"
  test.root = "features"
  tag.failures = "true"
  linked.tags = "issue"
  #  logging = "VERBOSE"
  browser.maximized = true
  reports = ["single-page-html", "navigator"]
  restart.browser.each.scenario=true;
}
